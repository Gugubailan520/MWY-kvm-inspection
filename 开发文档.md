# 违规巡查系统（KVM/LXC 网络监控）

## 一、项目概述

使用 Go 语言编写一套违规巡查系统，包含 Web 管理界面。系统分为**客户端（Agent）**和**服务端（Server）**两部分，用于实时监控宿主机上 KVM/LXC 虚拟机的网络活动，自动识别违规行为并存储日志，供巡查人员调取审查。

---

## 二、运行环境

### 客户端（Agent）
| 操作系统 | 版本 | 虚拟化平台 |
|---------|------|-----------|
| CentOS  | 7.9 / 8 | 魔方云（KVM） |
| Debian  | 12 / 13   | PVE（KVM/LXC） |

### 服务端（Server）
| 操作系统 | 版本 | 用途 |
|---------|------|-----|
| Ubuntu  | 24.04 / 25.04 | 数据中心 + Web 服务 |

---

## 三、功能需求

### 3.1 核心监控
- 实时捕获宿主机上所有 KVM/LXC 实例的网络流量（TCP/UDP 连接、包转发、DNS 请求等）
- 记录每个网络请求的六元组信息：**源 IP、源端口、目的 IP、目的端口、协议、方向（入站/出站）**
- 关联请求到具体的虚拟机实例（通过虚拟网桥或 MAC 地址映射）
- **域名监控**：捕获 DNS 请求/响应，解析并记录域名与 IP 的映射关系，实时匹配违规域名黑名单
- **网页标题捕获**：解析 HTTP/HTTPS 响应中的 `<title>` 标签，记录网站标题，用于辅助判定是否为违规站点（如博彩、色情、翻墙代理等）
- **宿主机接口流量监控**（参考 cockpit-traffic-monitor）：读取 `/proc/net/dev`，按接口（物理网卡、网桥 `vmbr*`、虚拟 `veth*`、防火墙 `fwpr*`、TAP、VLAN、Bond 等）实时统计收发字节/速率/包数/错误/丢包，支持多时间跨度趋势图与接口类型筛选

### 3.2 违规巡查（重点）
- 根据预设规则自动评判每个请求是否违规
- 违规规则示例：
  - 访问黑名单 IP 或域名（支持通配符，如 `*.example.com`）
  - 连接违规端口（如已知矿池端口、未授权代理端口）
  - 异常大流量 / 异常频率请求
  - 非允许时段的网络活动
  - DNS 解析到恶意域名
  - 网页标题命中违规关键词（博彩、色情、翻墙、VPN 代理等），标记为疑似违规
  - 识别出代理/隧道协议（Shadowsocks、Trojan、VMess、SOCKS5 等），标记为高风险违规
- 判定结果写入日志并触发告警

### 3.3 日志与查询
- 所有网络事件实时推送至服务端存储
- Web UI 支持按时间、节点、违规类型、IP 等多维度查询
- 支持日志导出

### 3.4 节点管理
- Web UI 中添加/删除监控节点
- 每个节点注册后由服务端生成唯一通信密钥（API Key）
- 客户端凭密钥向服务端上报数据

### 3.5 域名拉黑（iptables 联动）
- Web UI 中支持添加/删除黑名单域名或 IP
- 服务端将黑名单同步下发至指定节点的 Agent
- Agent 解析域名得到 IP 后，调用 `iptables` 在宿主机 `FORWARD` 链上插入 DROP/REJECT 规则，拦截对应流量
- 同时支持通过 `ip6tables` 封禁 IPv6 地址
- 黑名单生效后，对应流量在宿主机网络层直接被丢弃，KVM/LXC 实例无法访问
- 支持一键解封，Agent 收到指令后移除对应 iptables 规则

### 3.6 代理协议识别（基于 OpenGFW）
- 参考 [OpenGFW](https://github.com/apernet/OpenGFW) 项目的协议指纹识别实现，在 Agent 侧对流量进行深度包检测（DPI）
- 可识别的代理/隧道协议包括但不限于：
  - **Shadowsocks**（aes-256-gcm、chacha20-ietf-poly1305 等加密套件）
  - **Trojan** / **Trojan-Go**
  - **VMess** / **VLESS**（V2Ray / Xray 协议）
  - **HTTP 代理** / **SOCKS4/SOCKS5**
  - **SSH 隧道**（端口转发）
  - **TLS 伪装代理**（Reality、XTLS 等）
- 识别到代理协议后，记录协议类型并标记为高风险违规，实时告警
- 可联动 iptables 自动封禁对应 IP 或端口

---

## 四、技术栈

### 客户端（Agent）
| 组件 | 技术选型 |
|------|---------|
| 语言 | Go 1.22+ |
| 网络抓包 | `gopacket` / `libpcap`（基于 PF_RING 或 AF_PACKET 降低开销） |
| 协议识别 | DPI 深度包检测，参考 OpenGFW 实现（指纹匹配、TLS 握手分析、加密流量特征检测） |
| 临时存储 | SQLite（默认）或 JSON 文本文件，用于网络中断时的本地缓冲 |
| 通信 | WebSocket / gRPC 长连接，向服务端实时推送 |

### 服务端（Server）
| 组件 | 技术选型 |
|------|---------|
| 语言 | Go 1.22+ |
| HTTP 框架 | Gin 或 Echo |
| 业务数据库 | MySQL 8.0（存储节点信息、用户、规则配置） |
| 日志数据库 | MongoDB 7.0（存储海量网络事件日志，利用文档模型灵活查询） |
| 前端框架 | 火山引擎前端框架（Arco Design） |
| 图标库 | Font Awesome（fa 库） |
| 实时推送 | WebSocket（服务端 → 前端实时展示） |

### 前后端通信方式
- Web UI 通过 RESTful API 管理节点和查询日志
- 实时监控数据通过 WebSocket 从服务端推送至前端
- 客户端（Agent）与服务端通过 WebSocket 或 gRPC 长连接通信

---

## 五、数据流架构

```
┌──────────────┐    WebSocket/gRPC     ┌──────────────┐    WebSocket     ┌──────────────┐
│  Agent 客户端  │ ←──────────────────→ │   Go 服务端    │ ←─────────────→ │   Web 前端    │
│ (宿主机抓包)   │  数据上报 / 规则下发    │  (Gin/Echo)   │  查询 / 实时推送  │ (Arco Design) │
└──────┬───┬───┘                       └──────┬───────┘                 └──────────────┘
       │   │                                  │
       │   │ iptables 封禁                     │  持久化
       │   ▼                                  ▼
       │  ┌──────────┐              ┌──────────────────┐
       │  │ iptables  │              │  MongoDB (日志)    │
       │  │(内核过滤) │              │  MySQL (配置/节点)  │
       │  └──────────┘              └──────────────────┘
       │ 本地缓冲
       ▼
┌──────────────┐
│    SQLite    │
│  (离线缓存)   │
└──────────────┘
```

---

## 六、数据库设计概要

### 6.1 MySQL（业务数据）
- `nodes` — 节点信息（ID、名称、IP、操作系统、虚拟化类型、API Key、状态、最后心跳时间）
- `users` — 巡查人员账号（用户名、密码哈希、角色）
- `violation_rules` — 违规规则配置（规则名、类型、匹配条件、启用状态）
- `alerts` — 告警记录（节点ID、违规类型、摘要、时间、处理状态）
- `blacklist` — 黑名单记录（域名/IP、封禁类型、生效节点ID列表、创建时间、状态）

### 6.2 MongoDB（日志数据）
- 集合 `network_events`：
  ```json
  {
    "timestamp": "ISO8601",
    "node_id": "xxx",
    "vm_id": "kvm-实例UUID",
    "src_ip": "192.168.1.100",
    "src_port": 54321,
    "dst_ip": "8.8.8.8",
    "dst_port": 443,
    "protocol": "TCP",
    "direction": "outbound",
    "domain": "www.example.com",
    "title": "示例网站首页",
    "detected_protocol": "trojan",
    "bytes_sent": 1024,
    "bytes_received": 4096,
    "is_violation": true,
    "violation_type": "blacklist_ip",
    "violation_detail": "访问黑名单IP: 8.8.8.8"
  }
  ```

---

## 七、项目目录结构

```
kvm-inspection/
├── agent/                    # 客户端代码
│   ├── cmd/main.go           # 入口
│   ├── capture/              # 网络抓包模块
│   ├── reporter/             # 数据上报模块
│   └── storage/              # 本地缓存（SQLite）
├── server/                   # 服务端代码
│   ├── cmd/main.go           # 入口
│   ├── api/                  # RESTful API 路由
│   ├── ws/                   # WebSocket 处理
│   ├── service/              # 业务逻辑
│   ├── model/                # 数据模型
│   └── violation/            # 违规判定引擎
├── web/                      # 前端代码
│   └── src/
├── config/                   # 配置文件模板
├── deploy/                   # 部署脚本 & systemd 服务文件
├── go.mod
└── go.sum
```

---

## 八、性能要求

1. **CPU 占用**：客户端 Agent 在空闲状态下 CPU 占用 < 2%，高峰 < 5%
2. **内存占用**：客户端 Agent 常驻内存 < 100MB
3. **网络影响**：使用 AF_PACKET 零拷贝抓包，避免对宿主机网络栈产生影响
4. **采样策略**：支持按流量比例采样，高负载时自动降采样
5. **缓冲区**：客户端断网时本地 SQLite 缓冲不超过 500MB，恢复后自动补传

---

## 九、安全要求

1. 客户端与服务端通信使用 TLS 加密
2. 每个节点分配唯一 API Key，服务端验证后方可上报数据
3. Web UI 需登录认证，支持 RBAC 角色权限（管理员 / 巡查员）
4. 敏感配置（数据库密码、密钥）通过环境变量或配置文件注入，不硬编码